/**
 * Fond racine — `HuskoBackground` (dégradé principal = `theme.colors.gradient`).
 */
export const huskoRootBackgroundVisual = {
  topGoldWash: ['rgba(240, 208, 80, 0.07)', 'transparent'] as const,
  bottomVignette: ['transparent', 'rgba(0, 0, 0, 0.55)'] as const,
  dimOverlay: 'rgba(0, 0, 0, 0.12)',
} as const;
